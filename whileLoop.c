#include <stdio.h>

int main() {

	int upper, lower, step, iterP;
	upper = 100;
	lower = 0;
	step = 10;
	iterP = 0;

	while(iterP<=) {
		iterP+=step;
		printf("Iterator value is now: %d\n", iterP);
	}

	printf("Operation completed");

}
